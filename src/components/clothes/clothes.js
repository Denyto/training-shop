import { Link } from "react-router-dom";

function Clothes(p) {
    console.log(p);
  return (
    <section  className="clothes">
      <Link to='/'>{}</Link>
    </section>
  );
}

export default Clothes;
